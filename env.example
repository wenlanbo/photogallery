# Google Drive API Configuration
# For local development: Use Application Default Credentials (no env vars needed)
# For production (Vercel): Use OAuth credentials

# OAuth 2.0 Credentials (for production deployment)
GOOGLE_CLIENT_ID=your-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-oauth-client-secret

# Google Drive Folder ID (optional - if not set, will search all accessible folders)
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here

# For Vercel deployment, add these environment variables in your Vercel dashboard 